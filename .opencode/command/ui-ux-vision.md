---
description: UI/UX 界面分析 — 组件清单、设计 token、可访问性
---

调用 `npx linger-image-plugin ui-ux-vision "<界面截图>" ["关注点"]` 从设计系统角度分析界面。默认输出六个维度：布局与层级、组件识别、设计系统（色彩/字体/圆角/阴影以 CSS 变量形式给出）、交互模式、响应式、可访问性。用于界面截图、设计稿、前端还原前的读稿。一次传一个界面，长页面先切分。照图写前端时先跑这个拿到结构化的组件清单和 token，再写代码。详见 `/ui-ux-vision`。
