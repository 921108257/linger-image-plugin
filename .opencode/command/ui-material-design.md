---
description: UI 素材设计提示词生成 — 输出 Midjourney / SD / DALL-E 提示词
---

调用 `linger-image ui-material-design "<参考图>" ["素材需求"]` 根据参考图生成 AI 绘图提示词。输出三段：核心提示词（30-50 词英文，主题/风格/配色/关键元素）、参数建议（推荐模型、--ar 比例、--stylize / --chaos / --quality）、三个变体方向（换配色/换风格/换密度）。每个变体单独成块，可直接复制进生成器。参考图是风格来源，不是要复刻的内容。说清素材类型（图标/插画/背景/banner），类型不同参数差别很大。详见 `/ui-material-design`。
